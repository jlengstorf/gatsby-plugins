import React from 'react';

const HardCoded = () => (
  <>
    <h1>This title is hard-coded</h1>
    <p>Content in this component is hard-coded in the component file itself.</p>
  </>
);

export default HardCoded;
