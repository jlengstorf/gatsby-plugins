import React from 'react';

const Unstructured = () => (
  <>
    <h1>This is a hard-coded page</h1>
    <p>All of the data is embedded directly into the React component.</p>
  </>
);

export default Unstructured;
